{
  "name": "pastila",
  "version": "1.0.0",
  "description": "a electron a that takes notes via markdown and github gist.",
  "main": "index.js",
  "scripts": {
    "start": "electron app/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd app && npm install",
    "build": "atom-shell-packager app/ Pastila"
  },
  "keywords": [
    "electron"
  ],
  "author": "Jacob Lowe",
  "license": "MIT",
  "devDependencies": {
    "atom-shell-packager": "^2.1.1",
    "electron-prebuilt": "^0.24.0"
  }
}
